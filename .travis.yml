dist: trusty
sudo: false
language: r
cache: packages

r:
  - oldrel
  - release
  - devel

r_packages:
  - covr

after_success:
  - if [[ "${TRAVIS_R_VERSION}" == "stable" ]]; then Rscript -e 'covr::coveralls()'; fi
